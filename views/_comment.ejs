<div class="post-comment-item" id="comment-<%= comment._id %>">
  <div class="post-comment-header">
    <small>
      <% if(locals.user && locals.user.id == comment.user.id ){%>

      <button>
        <a
          class="delete-comment-button"
          href="/comments/destroy/<%= comment.id %>"
          >X</a
        >
      </button>
      <% } %>
    </small>
    <!-- <%= (comment.user);%> -->
    <!-- //why  ia m not getting name -->
    <!-- <%=(locals.user);%> -->
    <small> <%= (comment.user.name) %> </small>
    <!-- CHANGE :: display the likes of this comment, if the user is logged in, then show the link to toggle likes, else, just show the count -->
    <!-- study about the data attribute -->

    <small>
      <% if (locals.user){ %>
      <a
        class="toggle-like-button"
        data-likes="<%= comment.likes.length %>"
        href="/likes/toggle/?id=<%=comment._id%>&type=Comment"
      >
        <%= comment.likes.length %> Likes
      </a>

      <% }else{ %> <%= comment.likes.length %> Likes <% } %>
    </small>
  </div>
  <div className="post-comment-content"><%= comment.content %></div>
</div>
